name: Deploy Games

on:
  push:
    branches: ["main2"]
    paths:
      - "games/**"

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages-main2-games"
  cancel-in-progress: true

jobs:
  deploy-games:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout ultra-minimal (games only)
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          filter: blob:none
          sparse-checkout: |
            games
          sparse-checkout-cone-mode: true

      - name: Prepare deploy folder (contents only)
        run: |
          mkdir -p public
          cp -r games/* public/  # <- copy contents instead of folder

      - name: Upload artifact (fast)
        uses: actions/upload-pages-artifact@v3
        with:
          path: public

      - name: Deploy
        uses: actions/deploy-pages@v4
